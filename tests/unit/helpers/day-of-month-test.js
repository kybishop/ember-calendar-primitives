import { dayOfMonth } from 'dummy/helpers/day-of-month';
import { module, test } from 'qunit';

module('Unit | Helper | day of month');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = dayOfMonth([42]);
  assert.ok(result);
});
