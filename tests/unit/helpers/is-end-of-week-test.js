import { isEndOfWeek } from 'dummy/helpers/is-end-of-week';
import { module, test } from 'qunit';

module('Unit | Helper | is end of week');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isEndOfWeek([42]);
  assert.ok(result);
});
