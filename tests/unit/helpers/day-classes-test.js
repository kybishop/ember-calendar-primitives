import { dayClasses } from 'dummy/helpers/day-classes';
import { module, test } from 'qunit';

module('Unit | Helper | day classes');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = dayClasses([42]);
  assert.ok(result);
});
