import { isStartOfWeek } from 'dummy/helpers/is-start-of-week';
import { module, test } from 'qunit';

module('Unit | Helper | is start of week');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isStartOfWeek([42]);
  assert.ok(result);
});
