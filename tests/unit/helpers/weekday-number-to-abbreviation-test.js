import { weekdayNumberToAbbreviation } from 'dummy/helpers/weekday-number-to-abbreviation';
import { module, test } from 'qunit';

module('Unit | Helper | weekday number to abbreviation');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = weekdayNumberToAbbreviation([42]);
  assert.ok(result);
});
