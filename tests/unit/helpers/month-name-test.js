import { monthName } from 'dummy/helpers/month-name';
import { module, test } from 'qunit';

module('Unit | Helper | month name');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = monthName([42]);
  assert.ok(result);
});
