<vbox class="ecp--month-view">
  {{#each datesToDisplayByWeek as |weekOfDates|}}
    <hbox class="ecp--week">
      {{#each weekOfDates as |date|}}
        {{yield date}}
      {{/each}}
    </hbox>
  {{/each}}
</vbox>
